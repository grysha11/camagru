DB_URL=""
BACKEND_PORT=""
POSTGRES_USER=""
POSTGRES_PASSWORD=""
POSTGRES_DB=""
JWT_SECRET=""
APP_BASE_URL=""
# dev (make dev): SMTP_HOST=mailpit, SMTP_PORT=1025 (caught locally, never actually delivered)
# prod (make prod): needs a real SMTP relay (e.g. Mailgun/SendGrid/Brevo) — mailpit isn't reachable outside the dev network
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""
GITHUB_REDIRECT_URL=""
UPLOAD_PATH=""
TEST_DB_URL="postgres://<user>:<password>@localhost:5433/camagru_test?sslmode=disable"